[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sumdoc"
version = "0.2.2"
description = "Small Unix-style document conversion tools in one multicall command."
readme = "README.md"
requires-python = ">=3.10"
license = "GPL-2.0-or-later"
license-files = ["LICENSE", "COPYING"]
authors = [
    {name = "William Martinez Bas", email = "metfar@gmail.com"},
]
keywords = ["pdf", "markdown", "html", "images", "ocr", "ascii", "ansi", "braille", "semigraphics", "unix", "multicall"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Text Processing :: Markup",
    "Topic :: Utilities",
]
dependencies = [
    "Markdown>=3.5",
    "markdownify>=0.11",
]

[project.scripts]
sumdoc = "sumdoc.cli:entry_point"
markdown2helpdb = "sumdoc.tools.helpconv:markdown2helpdb_entry_point"
helpdb2markdown = "sumdoc.tools.helpconv:helpdb2markdown_entry_point"

[project.optional-dependencies]
image = [
    "Pillow>=10",
]
pdf = [
    "PyMuPDF>=1.22",
    "pdfminer.six>=20221105",
]
markdown = [
    "WeasyPrint>=60",
]
ocr = [
    "opencv-python>=4.8",
    "pytesseract>=0.3.10",
    "img2table>=1.2",
    "tabulate>=0.9",
]
all = [
    "Pillow>=10",
    "PyMuPDF>=1.22",
    "pdfminer.six>=20221105",
    "WeasyPrint>=60",
    "opencv-python>=4.8",
    "pytesseract>=0.3.10",
    "img2table>=1.2",
    "tabulate>=0.9",
]
dev = [
    "build>=1.2",
    "pytest>=8",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.setuptools.package-data]
sumdoc = ["styles/*.css"]
